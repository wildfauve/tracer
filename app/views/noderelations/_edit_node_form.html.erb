<div class="border-form">
	<%= form_for @node do |form| %>
		<% if @node.errors.any? %>
			<div id="error_explanation">
				<h2><%= pluralize(@node.errors.count, "error") %> prohibited the type from being saved:</h2>

				<ul>
					<% @rel.errors.full_messages.each do |msg| %>
					<li><%= msg %></li>
					<% end %>
				</ul>
			</div>
		<% end %>
		<div class="field">
			<%= form.label :name %>
			<%= form.text_field :name %>
		</div>
		Properties
		<div class="field">
			<%= form.fields_for :propinstances do |ff| %>
				<p>
					<%= ff.object.name%>
					 <%= ff.label :value, "Value" %><br />
					 <%= ff.text_field :value %><br />
			  </p>
			<% end %>
			
		</div>
		<div>
			<p>
				<%= form.submit "Update", :class => "form-submit-button" %>
			</p>
		</div>
		
	<% end %>
</div>